# Core ML / embeddings
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4

# LLM (mock mode works without API key)
openai>=1.0.0,<2.0.0

# LangChain (RAG utilities)
langchain>=0.1.0,<0.2.0
langchain-community>=0.0.10

# LLM monitoring
evidently==0.4.16

# Experiment tracking
mlflow==3.11.1

# Dashboard
streamlit==1.54.0
plotly==5.18.0

# Data
pandas==2.0.3
numpy>=1.24.0,<2.0.0

# Testing
pytest==9.0.3
pytest-cov==4.1.0
pytest-asyncio>=0.21.0

# Utilities
python-dotenv>=1.0.0
pillow>=10.0.0

# V2 — Streaming API (optional: required only if launching the FastAPI server)
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0

# V2 — LangGraph (optional: multi-agent orchestration without LangGraph falls back to built-in)
# langgraph>=0.0.30
